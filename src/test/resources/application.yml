spring:
  liquibase:
    enabled: true
    drop-first: false
    change-log: db-migrations/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
